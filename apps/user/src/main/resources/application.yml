server:
  port: 8083

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/user_db"
    username: root
    password: root
    initialization-mode: always
  jpa:
    hibernate:
      ddl-auto: update
  output:
    ansi:
      enabled: always
